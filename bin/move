#!/usr/bin/env ruby

require_relative '../lib/move.rb'

puts "Welcome to Tic Tac Toe!
Where would you like to go?"
user_input = gets.strip
def input_to_index(user_input)
  input.to_i
end
if input_to_index == 1 or input_to_index == 2
  puts display_board(move)
  else
    puts "Invalid"
  end
