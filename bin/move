#!/usr/bin/env ruby

require_relative '../lib/move.rb'

puts "Welcome to Tic Tac Toe!
Where would you like to go?"
input = gets.strip
def input_to_index(x)
  x.to_i
if input == 1 or input == 2
  puts display_board(input)
  else
    puts "Invalid"
  end
